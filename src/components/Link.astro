---
const { node } = Astro.props;
const href = node.href ?? node.markDef?.href ?? '#';
const external = /^https?:\/\//.test(href);
---
<a href={href} target={external ? '_blank' : undefined} rel={external ? 'noopener' : undefined}><slot /></a>
