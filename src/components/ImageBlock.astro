---
import { urlFor } from '../lib/sanity';
const { node } = Astro.props;
const src = urlFor(node).width(1400).url();
---
<figure>
  <img src={src} alt={node.alt ?? ''} loading="lazy" />
  {node.caption && <figcaption>{node.caption}</figcaption>}
</figure>
